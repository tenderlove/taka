
###
# This Ruby source file was generated by test-to-ruby.xsl
# and is a derived work from the source document.
# The source document contained the following notice:
=begin

Copyright (c) 2001 World Wide Web Consortium,
(Massachusetts Institute of Technology, Institut National de
Recherche en Informatique et en Automatique, Keio University). All
Rights Reserved. This program is distributed under the W3C's Software
Intellectual Property License. This program is distributed in the
hope that it will be useful, but WITHOUT ANY WARRANTY; without even
the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR
PURPOSE.
See W3C License http://www.w3.org/Consortium/Legal/ for more details.

=end
#

require 'dom_test_case'

###
#     The "insertBefore(newChild,refChild)" method inserts the
#     node "newChild" before the node "refChild". 
#     
#     Insert a newly created Element node before the eigth
#     child of the second employee and check the "newChild"
#     and "refChild" after insertion for correct placement.
# @author NIST
# @author Mary Brady
# see[http://www.w3.org/TR/1998/REC-DOM-Level-1-19981001/level-one-core#ID-952280727]
##
DOMTestCase('nodeinsertbefore') do

  ###
  # Constructor.
  # @param factory document factory, may not be null
  # @throws org.w3c.domts.DOMTestIncompatibleException Thrown if test is not compatible with parser configuration
  ##
  def setup
    ##
    ##   check if loaded documents are supported for content type
    ##
    contentType = getContentType()
    preload(contentType, "staff", true)
  end

  ###
  # Runs the test case.
  # @throws Throwable Any uncaught exception causes test to fail
  #
  def test_nodeinsertbefore
    doc = nil
    elementList = nil
    employeeNode = nil
    childList = nil
    refChild = nil
    newChild = nil
    child = nil
    childName = nil
    length = nil
    insertedNode = nil
    actual = []
      
    expectedWithWhitespace = []
      expectedWithWhitespace << "#text"
      expectedWithWhitespace << "employeeId"
      expectedWithWhitespace << "#text"
      expectedWithWhitespace << "name"
      expectedWithWhitespace << "#text"
      expectedWithWhitespace << "position"
      expectedWithWhitespace << "#text"
      expectedWithWhitespace << "newChild"
      expectedWithWhitespace << "salary"
      expectedWithWhitespace << "#text"
      expectedWithWhitespace << "gender"
      expectedWithWhitespace << "#text"
      expectedWithWhitespace << "address"
      expectedWithWhitespace << "#text"
      
    expectedWithoutWhitespace = []
      expectedWithoutWhitespace << "employeeId"
      expectedWithoutWhitespace << "name"
      expectedWithoutWhitespace << "position"
      expectedWithoutWhitespace << "newChild"
      expectedWithoutWhitespace << "salary"
      expectedWithoutWhitespace << "gender"
      expectedWithoutWhitespace << "address"
      
    expected = []
      
    doc = load_document("staff", true)
      elementList = doc.getElementsByTagName("employee")
      employeeNode = elementList.item(1)
      childList = employeeNode.childNodes()
      length = childList.length()
      
      if (equals(6, length))
        refChild = childList.item(3)
      expected =  expectedWithoutWhitespace
          else
            refChild = childList.item(7)
      expected =  expectedWithWhitespace
         end
       newChild = doc.createElement("newChild")
      insertedNode = employeeNode.insertBefore(newChild, refChild)
      indexid5967285 = 0
    while (indexid5967285 < childList.length)
      child = childList.item(indexid5967285)
    childName = child.nodeName()
      actual << childName
      indexid5967285 += 1
            end
      assert_equal(expected, actual, "nodeNames")
            
  end

  ###
  # Gets URI that identifies the test.
  # @return uri identifier of test
  #
  def targetURI
    "http://www.w3.org/2001/DOM-Test-Suite/tests/Level-1/nodeinsertbefore"
  end
end

